# Optional: Required for image upload feature
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# App runs without this key (barcode scan + link paste will still work)
# Vision parsing will return helpful error if key is missing
